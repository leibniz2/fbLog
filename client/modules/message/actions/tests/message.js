const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../message';

describe('message.actions.message', () => {
  it('should do something');
});
